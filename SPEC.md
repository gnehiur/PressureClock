# 桌面时间进度钟 Specification v0.2

## 1. 产品概述

### 1.1 产品名称
暂定名：桌面时间进度钟（Desktop Time Pressure Clock）

### 1.2 产品一句话定义
一个极简的 macOS 桌面时钟应用：上方显示 24 小时制当前时间，下方显示日期，再下方用一组极简进度条把“今天 / 本周 / 本月 / 本年 / 本世纪”已经流逝的时间可视化，用持续、冷静、不可逆的方式提醒用户时间正在消耗。

### 1.3 核心价值
这不是“告诉用户现在几点”的工具，而是“让用户对时间流逝有即时体感”的工具。它要解决的不是信息缺失，而是注意力麻木。时钟提供时刻感，进度条提供损耗感，两者结合才会形成时间压力。

### 1.4 设计原则
1. 极简优先：只显示最关键的信息，不做花哨装饰，不做拟物，不做游戏化。
2. 一眼读懂：用户 0.5 秒内必须能看出“现在几点”和“今天已经过去多少”。
3. 冷静压迫感：不用红色警报或弹窗打断，而是用不可逆进度条制造持续压力。
4. 真实时间模型：所有进度都基于真实日历区间计算，而不是偷懒用固定秒数硬算。
5. 桌面工具感：能像普通窗口一样使用，也能切换为悬浮在其他 App 之上。

## 2. 目标与边界

### 2.1 目标
1. 提供一个常驻桌面的极简时间视图。
2. 通过时间进度条强化用户的时间意识、紧迫感和执行感。
3. 支持普通窗口模式与悬浮模式切换。
4. 支持窗口缩放，并记忆窗口大小与位置。
5. 允许用户决定显示哪些时间维度：今天 / 本周 / 本月 / 本年 / 本世纪 / 自定义。

### 2.2 非目标
1. 不做系统 Widget。原因是 WidgetKit 不适合高频实时刷新，也不适合这种常驻、可交互、可悬浮的窗口体验。
2. 不做任务管理器、番茄钟、待办列表整合。这个产品的核心是“时间感知”，不是“任务系统”。
3. 首版不做复杂主题市场、联网同步、账号系统。
4. 首版不做通知轰炸。进度本身就是提醒，不用再叠加打断。

## 3. 用户画像与场景

### 3.1 目标用户
1. 对拖延敏感、希望建立时间意识的人。
2. 需要长时间坐在电脑前工作的创作者、开发者、学生、独立工作者。
3. 想要“始终看见时间成本”而不是“偶尔看一眼系统时钟”的人。

### 3.2 典型场景
1. 写代码、写文档、剪视频时，把窗口悬浮在其他 App 上方，持续感受“今天正在过去”。
2. 做深度工作时，将窗口缩小放在屏幕角落，只保留时间和 2-3 条关键进度。
3. 每天开机后让它恢复到上次位置与大小，形成稳定的工作环境线索。

## 4. 核心体验

用户打开应用后，看到一个黑底极简窗口：

1. 第一层信息：大号时间，24 小时制，例如 `15:15:07`。
2. 第二层信息：当前日期，例如 `3月23日，星期一`。
3. 第三层信息：若干条“时间流逝进度条”，例如 Hour / Day / Week / Month / Year / Century。
4. 用户可以将窗口设为普通窗口，也可以一键切换为“始终悬浮在最前”。
5. 用户可以拖动、缩放窗口；下次启动时恢复上次布局。

## 5. 信息架构与界面结构

### 5.1 布局结构
建议纵向单列布局，自上而下：

1. 时间区
2. 日期区
3. 进度条组
4. 可选的轻量控制入口（设置按钮或右键菜单）

### 5.2 视觉结构
1. 背景：纯黑或接近纯黑，支持透明度调节。
2. 时间：超大号白字，使用等宽数字（monospaced digits），避免秒跳动时宽度抖动。
3. 日期：中号白字或低一级灰白。
4. 进度条：细线型，白色或浅灰色填充，尽量克制。
5. 标签：尽量小，弱化干扰，但不能缺失识别性。

### 5.3 首版推荐界面草图

```text
15:15:07
3月23日，星期一

HOUR
15:00  [██████--------------]   16:00

TODAY
0.00   [██████████████------]   24.00

WEEK
周一   [████████------------]   周日

MONTH
1日    [██████████----------]   31日

YEAR
1月    [████----------------]   12月

CENTURY
2001   [████----------------]   2100
```

说明：
1. 只有 Day 这条进度明确使用 `0.00 -> 24.00` 的时间轴。
2. 其他进度条使用各自最自然的边界标签，避免所有条都机械写成 `0 -> 100`。
3. 标签放在进度条上方，能兼顾极简和可读性。

## 6. 功能需求

### 6.1 时间显示
1. 默认采用 24 小时制。
2. 首版默认显示 1 位小数秒，格式为 `HH:mm:ss.S`。
3. 支持 0-3 位小数秒精度切换，默认值为 1 位小数秒。
4. 时间应按用户当前系统时区显示。

#### 为什么默认带 1 位小数秒
1 位小数秒本质上是 `0.1 秒` 精度，也就是每 `100ms` 变化一次。它比纯秒级更有“流逝感”，但又没有 2-3 位小数秒那样强的视觉噪声。这个产品的目标是让用户持续感到时间在滑走，所以默认 1 位小数秒是一个更贴近目标的取舍：压迫感足够明显，但不会立刻变成秒表。

### 6.2 日期显示
1. 默认格式：`M月d日，EEEE`，例如 `3月23日，星期一`。
2. 跟随系统语言和地区格式，但首版默认中文文案优先。
3. 日期区字号明显小于时间区，但仍需足够清晰。

### 6.3 窗口模式
应用支持两种窗口模式：

1. 普通窗口模式
   - 行为接近普通 macOS 窗口。
   - 支持拖动、缩放、最小化、关闭。
   - 不强制置顶。

2. 悬浮窗口模式
   - 窗口保持在其他应用前面。
   - 本质是把窗口层级设置为 macOS 的 floating level，而不是做成系统 Widget。
   - 仍然支持拖动、缩放。

### 6.4 窗口尺寸与位置
1. 用户可以自由调整窗口大小。
2. 应用关闭或退出后，应记忆最后一次窗口位置、尺寸、悬浮状态。
3. 再次启动时自动恢复。
4. 需要限制最小窗口尺寸，避免内容挤压到不可读。

### 6.5 进度条系统

#### 6.5.1 内置进度条类型
首版内置以下类型：
1. 本小时（Hour）
2. 今天（Day）
3. 本周（Week）
4. 本月（Month）
5. 本年（Year）
6. 本世纪（Century）

#### 6.5.2 自定义进度条
首版直接支持用户配置自定义进度条。
1. MVP 必须支持固定区间：例如 `2026-01-01` 到 `2030-12-31`。
2. 完整版本扩展支持周期区间：例如每季度、每半年、每十年。

自定义进度条在首版至少应支持以下配置字段：
1. 标题，例如 `30岁前人生进度`、`今年目标周期`。
2. 开始时间。
3. 结束时间。
4. 左端标签，可选；留空时自动根据开始时间生成。
5. 右端标签，可选；留空时自动根据结束时间生成。
6. 是否启用。
7. 排序位置。

自定义固定区间在结束后默认保持显示为 `100%`，不自动删除。原因是这个产品强调“时间已经过去”的体感，自动消失反而会削弱这种反馈。

#### 6.5.3 进度条显示元素
每条进度条包含：
1. 标题，例如 `TODAY`、`MONTH`。
2. 左端边界标签。
3. 右端边界标签。
4. 细线型进度轨道。
5. 当前进度填充。
6. 可选百分比文本（首版默认关闭，避免信息过载）。

#### 6.5.4 进度条排序与显隐
1. 用户可以开启/关闭某些条目。
2. 用户可以调整条目顺序。
3. 默认显示：Hour / Day / Week / Month / Year。
4. Century 默认关闭，但提供入口。

### 6.6 设置项
完整版本的设置页应包含：
1. 是否悬浮在其他 App 之上。
2. 时间精度：秒 / 1 位小数秒 / 2 位小数秒 / 3 位小数秒。
3. 背景透明度。
4. 显示哪些进度条。
5. 进度条排序。
6. 自定义进度条管理。
7. 启动时恢复上次窗口状态。

### 6.7 交互入口
首版可采用以下任一方案，优先简单实现：
1. 标准菜单栏中的 `Preferences...`
2. 窗口内一个低干扰设置按钮
3. 窗口右键菜单

推荐首版采用：标准菜单 + 简单设置窗口。原因是最稳定，也最符合 macOS 习惯。

## 7. 进度计算规格

### 7.1 通用公式
任意进度条的核心公式为：

```text
progress = clamp((now - intervalStart) / (intervalEnd - intervalStart), 0...1)
```

其中：
1. `now` 是当前时刻。
2. `intervalStart` 是当前时间所属区间的起点。
3. `intervalEnd` 是当前时间所属区间的终点。
4. `clamp` 用于把结果限制在 0 到 1 之间。

### 7.2 为什么不能偷懒用固定秒数
例如“一天”等于 86400 秒，这在大部分日子成立，但遇到夏令时切换时可能不是 24 小时。月份也有 28、29、30、31 天，年份也有闰年。为了保证进度真实，必须基于 `Calendar` 求真实区间，而不是写死常量。

### 7.3 各类区间定义

#### Day
1. 区间起点：本地当天 00:00:00。
2. 区间终点：下一天 00:00:00。
3. 左标签：`0.00`
4. 右标签：`24.00`

#### Week
1. 区间起点：按用户当前系统地区与日历设置的周起始日计算。
2. 区间终点：下一周起点。
3. 左标签：本周第一天简称。
4. 右标签：本周最后一天简称。

实现上应使用 `Calendar.autoupdatingCurrent` 或等价机制，读取系统的 `firstWeekday` 和地区语义，而不是写死为周一或周日。

#### Month
1. 区间起点：本月 1 日 00:00:00。
2. 区间终点：下月 1 日 00:00:00。
3. 左标签：`1日`
4. 右标签：本月最后一天日期，例如 `31日`。

#### Year
1. 区间起点：1 月 1 日 00:00:00。
2. 区间终点：下一年 1 月 1 日 00:00:00。
3. 左标签：`1月`
4. 右标签：`12月`

#### Century
1. 默认采用严格 Gregorian 计数：例如 21 世纪为 `2001-01-01` 到 `2101-01-01`。
2. 显示标签可写为 `2001` 和 `2100`，右侧标签用于表达“最后一个可见年份”，而不是数学意义上的排他终点。
3. 如果后续用户反馈普遍更习惯 `2000-2099` 的俗称世纪，可增加设置项，但首版先保持严格定义。

#### Custom
1. 固定区间：用户明确提供开始和结束时间。
2. 周期区间：根据周期模板为当前时刻推导所在区间。

## 8. 技术方案

### 8.1 技术选型
首版建议：
1. `SwiftUI` 负责界面声明式布局。
2. `AppKit` 负责原生窗口行为控制。
3. `UserDefaults` / `AppStorage` 持久化用户设置。

### 8.2 为什么不是纯 SwiftUI
SwiftUI 很适合做界面，但 macOS 上“窗口是否悬浮、窗口层级、窗口尺寸记忆、窗口行为细控”这些事情，本质仍然更接近 AppKit 能力。因此最合理的做法不是二选一，而是：UI 用 SwiftUI，窗口控制用 AppKit。

### 8.3 窗口机制
核心机制：
1. 用 `NSWindow` 承载内容。
2. 通过 `window.level = .normal` 与 `window.level = .floating` 切换普通/悬浮。
3. 通过窗口代理监听移动、缩放、关闭事件。
4. 在这些事件发生时，把窗口 frame 与设置写入持久化存储。

### 8.4 时间刷新机制
需要按显示精度选择刷新策略：

1. 默认模式（1 位小数秒）
   - 每 `100ms` 刷新一次。
   - 这是首版默认模式，能提供连续流逝感。

2. 秒级模式
   - 每 `1s` 刷新一次。
   - 适合觉得默认模式压力过强的用户。

3. 2 位小数秒模式
   - 每 `10ms` 刷新一次。
   - 仅在用户主动开启时使用。

4. 3 位小数秒模式
   - 不应简单粗暴地用真实 `1ms` 定时器强刷 UI。
   - 更合理的做法是按显示帧率或受控定时器刷新，并在每次绘制时读取当前时间。
   - 这样更符合显示硬件上限，也更省资源。

### 8.5 为什么 1ms 刷新没有意义
如果屏幕是 60Hz，一帧大约 16.7ms；120Hz 也大约 8.3ms。也就是说，就算你代码每 1ms 更新一次文本，用户肉眼和屏幕都不可能完整看到这些变化。UI 刷新应该贴近“可感知精度”，而不是盲目追求数值精度。

### 8.6 进度刷新机制
1. 内置的 Day / Week / Month / Year / Century 进度条默认按 `1s` 更新即可。
2. 时间文本可以高频刷新，但长周期进度条没必要跟着高频重绘。
3. 对于总时长小于等于 `24h` 的自定义固定区间，建议提升到 `100ms` 更新，这样短周期条目的流逝感会更自然。
4. 对于长于 `24h` 的自定义区间，维持 `1s` 更新即可。

## 9. 视觉与交互设计 v2

这一节不是“美术偏好”，而是产品可读性的正式规格。这个应用的核心矛盾不是“信息不够多”，而是“怎样在极少信息里，把时间压力表达得更清楚”。所以视觉设计必须服务于三个目标：一眼读出当前时刻、快速判断所处进度、在不打断工作的前提下保持持续压迫感。

### 9.1 当前版本存在的问题
当前实现的主要问题不是黑白配色，而是信息结构不自然：
1. 刻度只有线，没有文字锚点，导致用户看见了“分隔”，却不知道那些分隔对应什么时间。
2. 左右边界标签被放在条形两端，但与中间刻度没有形成一套完整坐标系，所以用户很难把“0.00 / 24.00”和中间某根线对应起来。
3. 所有进度条都使用同一种视觉语法，但“本小时 / 今日 / 本周 / 本月 / 本年”的时间粒度完全不同，应该使用不同的标注策略。
4. 进度条没有“当前语义提示”，例如今日这条应该能让用户立即读出“现在接近 18:00”，而不是只看到一段白条。
5. 控制按钮虽然已经做了隐藏逻辑，但视觉上仍然像“界面组件”，不像“背景中的工具”；后续需要进一步弱化。

### 9.2 视觉总方向
最佳方向不是堆更多元素，而是做成一种“仪表盘式极简时间标尺”。核心视觉语言如下：
1. 大时间负责“现在几点”。
2. 日期负责“今天是哪一天”。
3. 每条进度不是普通进度条，而是“带标尺的时间轴”。
4. 标尺只给“关键刻度”文字，不给每根刻度文字，避免变成表格。
5. 填充条只表达“已流逝”，刻度和文字负责表达“坐标意义”。
6. 任何附加说明都必须弱于时间本体，不能喧宾夺主。

### 9.3 总体布局
推荐保持单列纵向布局，但把每个进度条改成三层结构，而不是现在这种“标题一行 + 进度一行”的结构。

推荐布局：
1. 顶部大时间：`17:24:40.3`
2. 次级日期：`3月23日，星期一`
3. 进度组：若干个“时间轴行”，默认顺序为 `本小时 -> 今日 -> 本周 -> 本月 -> 今年 -> 自定义`
4. 控制区：仅在窗口激活时淡入，平时不占视觉注意力

进度组里的单行结构统一为：
1. 标题行：左边是条目名，右边是当前语义值
2. 标尺标签行：显示关键刻度的文字标签
3. 时间轴行：底轨、次刻度、主刻度、当前进度填充、当前时间位置指针

也就是说，视觉顺序必须是：
1. 先告诉用户“这是哪一类时间”
2. 再告诉用户“这根轴上的关键点是什么”
3. 最后再用填充长度表达“现在走到了哪”

### 9.4 最终推荐的单条设计
每条进度条采用以下结构：

```text
今日                                  17:24 / 24:00
00:00         06:00         12:00         18:00         24:00
| · · · · · · | · · · · · · | · · · · · · | · · · · · · |
███████████████████████████████▌
```

说明：
1. 第一行是“标题 + 当前状态”，让用户不用估算就知道当前所处位置。
2. 第二行是关键刻度文字，只标主刻度，不标每一个次刻度。
3. 第三行是刻度系统，主刻度长、次刻度短。
4. 第四行是进度填充，必要时可以和第三行合并成一层，但语义上仍然要区分“标尺”和“流逝”。
5. 当前进度末端建议加一个极细的“现在指针”或亮点，用来强化“现在正在这里”。

这套结构比“左右端点 + 中间无语义小线”更专业，原因很简单：它把“数轴”这个用户已熟悉的认知模型完整建立起来了。用户不是在看一根随机长度的白条，而是在看一根有坐标、有当前点的时间轴。

### 9.5 专业版视觉规则

#### 9.5.1 时间区
1. 时间必须继续保持全界面最高优先级。
2. 时间字体建议从“过于圆润的厚重感”调整为更中性的系统字重，例如 `semibold` 或 `bold`，但不建议继续加粗。
3. 数字必须保持 `monospacedDigit`，否则小数秒跳动时宽度变化会破坏稳定感。
4. 小数秒应弱于主时间，可以通过更浅透明度、略细字重或更小字号处理，而不是和 `HH:mm:ss` 完全同权。

推荐形式：
1. `17:24:40` 使用主字号。
2. `.3` 使用主字号的 `0.72-0.82` 倍，并降低不透明度。

这样做的原理是建立“主时间 + 高频余量”的层级。用户先读整秒级时间，再被小数秒持续提醒“它还在动”。

#### 9.5.2 日期区
1. 日期要从“第二个大标题”降级为“时间的注释层”。
2. 推荐格式改为：`2026年3月23日 周一` 或 `3月23日 周一`，不必再用顿号感太强的标点。
3. 日期字号建议为主时间的 `0.20-0.24`，并使用较低对比度。

#### 9.5.3 进度区
1. 不再使用左右大标签把进度条夹在中间的布局，这会把条挤短，也会破坏视觉连贯性。
2. 改为整条通栏时间轴，让刻度和标签直接贴着轴分布。
3. 标题与当前语义值放在轴上方，起到“解释器”作用。
4. 关键刻度标签放在轴的正上方或正下方，与主刻度精确对齐。
5. 只给主刻度配文字，次刻度只保留线。

### 9.6 每类进度条的最佳标注方案

#### 9.6.1 本小时
本小时应该放在第一行。它不是“今日”的缩小版，而是一个战术时间轴：它让用户立刻感知“这个自然小时已经被吃掉多少”。如果说“今日”负责中尺度压迫，“本小时”负责短尺度启动和执行。

推荐规则：
1. 区间固定为当前自然小时，例如当前时间是 `21:03:20.1`，则本小时轴的区间为 `21:00 -> 22:00`。
2. 底层模型必须按 `60` 个等宽分钟格子建立；每个格子代表 `1` 分钟。这样不会出现“某一段看起来比另一段多一格”或边界语义混乱的问题。
3. 进度填充不是按整分钟跳，而是在当前分钟格子内部按秒和小数秒连续推进；也就是说，语义层是 `60` 个离散分钟单元，渲染层仍然是连续流逝。
4. 主刻度文字默认使用当前小时的四分点：`21:00 / 21:15 / 21:30 / 21:45 / 22:00`。
5. 次刻度默认每 `5` 分钟一根；`15` 分钟刻度比普通 `5` 分钟刻度更长、更亮。
6. 当前状态文案默认显示为 `21:03 / 22:00`，也就是“现在在这个小时里的位置 / 这一小时结束点”。
7. 可选副文案可以是 `已过 03:20` 或 `已过 5.6%`，但默认不显示，避免和主时间争夺注意力。

为什么这样设计：
1. “一个小时”最容易让用户进入行动状态，但前提是刻度必须稳定、准确、可预测，所以底层必须是严格的 `60` 分钟格子模型。
2. 主标签选 `15` 分钟，而不是把 `60` 个分钟数字全写出来，是因为四分点是最普遍、最不费认知的时间锚点。
3. 次刻度选 `5` 分钟，而不是 `1` 分钟，是因为 `1` 分钟线在中小窗口会形成噪声；`5` 分钟已经足够让用户判断“还有几分钟到半点/整点”。
4. 这条轴将来天然可以兼容“30 分钟专注块”或“1 小时时间盒”模式，因为它的底层不是百分比轴，而是真实分钟轴。

#### 9.6.2 今日
今日是最重要的一条，因为它最能产生即时压力。它必须是信息最完整的一条。

推荐规则：
1. 主刻度：`00:00 / 06:00 / 12:00 / 18:00 / 24:00`
2. 次刻度：每小时一根
3. 当前状态文案：`17:24 / 24:00`
4. 可选副文案：`已过去 72.6%`

为什么不是只写 `0.00 -> 24.00`：
因为用户真正关心的不是数学端点，而是“现在接近中午、傍晚还是深夜”。`06:00 / 12:00 / 18:00` 这些语义刻度才有心理意义。`0.00` 和 `24.00` 可以保留，但不能是唯一文字。

#### 9.6.3 本周
本周的最佳语义单位不是“百分比”，而是“今天位于本周哪里”。

推荐规则：
1. 主刻度：每天一根
2. 文字标签：根据宽度显示 `周一 / 周三 / 周五 / 周日`，或在足够宽时显示每天
3. 次刻度：每天中间的半天刻度
4. 当前状态文案：`周一 17:24`

关键点：
1. 周起始日必须跟随系统地区。
2. 标签不能固定写死周一到周日的视觉顺序，而应由当前 `Calendar.firstWeekday` 决定。

#### 9.6.4 本月
月份的视觉难点在于天数变化大，不能把所有日期都写出来。

推荐规则：
1. 主刻度优先选择：`1日 / 8日 / 15日 / 22日 / 月末`
2. 在宽度很大时，可增加 `5` 或 `6` 个文字点位，例如 `1 / 6 / 12 / 18 / 24 / 月末`
3. 次刻度：每日一根
4. 当前状态文案：`23日`

核心原则：
月视图最适合“周级语义点 + 日级细刻度”的组合，而不是把所有日期都直接写在屏幕上。

#### 9.6.5 今年
年视图最自然的语言单位是月份，不是季度数字。

推荐规则：
1. 主刻度：`1月 / 4月 / 7月 / 10月 / 12月`
2. 次刻度：每月一根
3. 当前状态文案：`3月`
4. 足够宽时，可把主刻度密度提升为双月或逐月标注，但默认不建议全标 12 个月

#### 9.6.6 本世纪
本世纪不应该像前几条那样默认高优先级展示，因为语义跨度太大，日常压迫感弱。

推荐规则：
1. 主刻度：每 10 年
2. 次刻度：每 5 年
3. 当前状态文案：当前年份，例如 `2026`
4. 默认关闭，仅在用户开启后显示

#### 9.6.7 自定义固定区间
自定义区间要避免沦为“一条没有上下文的长条”。它也必须具备刻度语义。

推荐规则：
1. 标题使用用户输入的名称，例如 `30岁前人生进度`
2. 当前状态文案显示“当前时间在该区间中的当前位置”，例如 `剩余 1287 天` 或 `已过 61.2%`
3. 主刻度自动按区间跨度生成：
   - 小于 1 天：按小时
   - 1 天到 45 天：按天或周
   - 45 天到 2 年：按月或季度
   - 超过 2 年：按年或关键年份
4. 左右边界日期可以保留，但应该作为首尾锚点，不应替代中间刻度标签

### 9.7 自适应规则
这个产品不能做“固定一套标签”，因为用户会缩放窗口。真正专业的实现必须是响应式时间标尺，而不是响应式容器。

推荐把窗口宽度分成四档：
1. `Compact`：小于 `420pt`
2. `Medium`：`420-680pt`
3. `Large`：`680-980pt`
4. `XLarge`：大于 `980pt`

不同档位的策略：

#### Compact
1. 只显示最必要的 3 个主标签。
2. 只保留主刻度，不显示次刻度文字。
3. 当前状态文案保留。
4. `本小时` 在 Compact 下只显示 `起点 / 半点 / 终点`，例如 `21:00 / 21:30 / 22:00`，次刻度降级为每 `10` 分钟一根。
5. 默认仅展示 `本小时 / 今日 / 本周 / 本月` 四条，其他条目可滚动查看。

#### Medium
1. 显示 4-5 个主标签。
2. 次刻度保留线，不显示文字。
3. 本小时显示完整的 `15` 分钟主标签和 `5` 分钟次刻度。
4. 今日显示每小时次刻度，本周显示半天次刻度，本月显示每日次刻度。

#### Large
1. 显示完整主标签。
2. 本小时保持 `15` 分钟主标签，不额外增加文字密度，但当前分钟指针必须更醒目。
3. 可为“今日”增加更细的 3 小时辅助标签，前提是不拥挤。
4. 当前时间指针与填充终点一起显示。

#### XLarge
1. 可以在不拥挤的前提下增加更多语义标签。
2. 允许“本小时”额外显示 `10 / 20 / 40 / 50` 这些辅助分钟标签，但它们必须明显弱于 `00 / 15 / 30 / 45 / 60`。
3. 允许“今日”显示 `00 / 03 / 06 / 09 / 12 / 15 / 18 / 21 / 24`。
4. “本周”允许每天都显示文字。
5. “本月”允许 5-6 个日期标签。

自适应的原则不是“越大越多”，而是“保证任意相邻标签之间的最小像素距离”。如果达不到这个距离，就必须降级。这样用户永远不会看到互相打架的文字。

### 9.8 控件与界面噪声控制
控制区应该继续保持“默认隐藏，仅在激活时出现”，但视觉上还要更克制：
1. 平时完全隐藏底部按钮。
2. 激活窗口后，用低透明度淡入一排轻量胶囊按钮。
3. 右键菜单继续保留，作为高级入口。
4. 不在主体区域长期显示“右键窗口也能切换模式”这种教学文案；它可以只在首次使用时显示一次，或移入设置说明。

这是一个很关键的 UX 原则：教学信息不应该永久占据主界面。主界面属于时间，不属于帮助文案。

### 9.9 最终推荐版界面草图

```text
17:24:40.3
3月23日 周一

本小时                                    17:24 / 18:00
17:00         17:15         17:30         17:45         18:00
|  ·  ·  |  ·  ·  |  ·  ·  |  ·  ·  |
████████████████▍

今日                                      17:24 / 24:00
00:00         06:00         12:00         18:00         24:00
|  ·  ·  ·  · |  ·  ·  ·  · |  ·  ·  ·  · |  ·  ·  ·  · |
███████████████████████████████████▍

本周                                      周一 17:24
周一            周三            周五            周日
|    ·    |    ·    |    ·    |    ·    |    ·    |    ·    |
███████▏

本月                                      23日
1日            8日            15日            22日            31日
| · · · · · · | · · · · · · | · · · · · · | · · · · · · |
███████████████████████▊

今年                                      3月
1月            4月            7月            10月            12月
|   ·   |   ·   |   ·   |   ·   |   ·   |   ·   |
█████▍
```

### 9.10 交互细节建议
1. 鼠标悬停某条时间轴时，可以高亮该行的当前状态文案和当前指针，但不显示复杂 tooltip。
2. 双击时间区可切换是否显示小数秒。
3. 右键某条自定义进度条可快速编辑区间。
4. 滚轮或触控板缩放窗口时，布局密度自动重排，不出现标签重叠。

### 9.11 实现层设计原则
为了把这个设计真正做对，代码层必须采用三层模型，而不是“先画条再塞字”：
1. `ProgressSemantics`：定义每类时间轴应该有哪些候选主刻度、次刻度和文字标签。
2. `ScaleLayoutEngine`：根据窗口宽度、字体宽度、最小间距规则，从候选标签中筛出当前能安全显示的一组。
3. `ProgressAxisView`：负责把“标题 / 当前状态 / 标签 / 刻度 / 填充 / 当前指针”画出来。

这三个层次分开之后，设计就不会被代码绑死。以后你要换布局、换主题、换不同条目的标注策略，都不需要重写底层计算。

### 9.12 下一步实现建议
真正应该改的不是颜色，而是组件结构。后续实现顺序建议如下：
1. 先重构单条进度组件，做成“标题行 + 标签行 + 时间轴行”的三层结构。
2. 再加入“当前状态文案”，例如 `17:24 / 24:00`、`周一 17:24`、`23日`、`3月`。
3. 再实现按宽度档位进行标签筛选。
4. 最后再调字体、透明度、间距和控件显隐。

如果这四步顺序反过来做，就会陷入“调了很久还是丑”的常见 UI 陷阱，因为根结构没有被修正。

## 10. 数据模型建议

### 10.1 用户设置模型

```text
AppSettings
- windowMode: normal | floating
- windowFrame: CGRect
- timePrecision: second | tenth | hundredth | millisecond
- backgroundOpacity: Double
- progressItems: [ProgressItem]
- restoreWindowOnLaunch: Bool
```

### 10.2 进度条模型

```text
ProgressItem
- id: UUID
- kind: day | week | month | year | century | customFixed | customRecurring
- title: String
- isEnabled: Bool
- sortOrder: Int
- leftLabelStyle: LabelStyle
- rightLabelStyle: LabelStyle
- customConfig: Optional<CustomProgressConfig>
```

### 10.3 自定义进度配置

```text
CustomProgressConfig
- mode: fixed | recurring
- startDate: Date?
- endDate: Date?
- recurrence: none | quarter | halfYear | decade | customComponent
- leftLabelOverride: String?
- rightLabelOverride: String?
- keepVisibleAfterCompletion: Bool
```

## 11. 用户体验细节

### 11.1 视觉节奏
1. 时间最大。
2. 日期次一级。
3. 进度条细而稳定。
4. 空白要留够，让“时间”本身成为视觉主体。

### 11.2 压迫感的来源
这个产品的紧迫感不应该来自红色、抖动、弹窗，而应该来自下面三个机制：
1. 进度条持续推进且不可逆。
2. 长短不同的时间尺度同时出现，用户会被迫感受到“今天”和“这一年”都在流逝。
3. 应用始终在视野里，占用极少空间但持续存在。

### 11.3 为什么这个设计有效
普通时钟只传达“当前时间点”；进度条传达“剩余空间”。人对“空间被占满”的感知比对抽象数字更强。尤其是 `Today` 这条，当左边是 `0.00`，右边是 `24.00`，中间填充在持续增长时，用户会形成非常直观的“今天已经被吃掉多少”的感受。

## 12. 非功能需求

1. 启动快：冷启动目标 < 1 秒。
2. 占用轻：常驻时 CPU 与内存都要尽量低。
3. 稳定：长期挂在桌面上不能频繁卡顿或闪烁。
4. 可读：缩小窗口后仍能维持基本可读性。
5. 本地优先：首版不依赖网络。

## 13. MVP 范围

### 13.1 MVP 必做
1. 大号 24 小时制时间显示，默认带 1 位小数秒。
2. 日期显示。
3. 普通 / 悬浮模式切换。
4. 窗口缩放与位置记忆。
5. Hour / Day / Week / Month / Year 五条内置进度。
6. 自定义固定区间进度条。
7. 简单设置面板。

### 13.2 MVP 可选
1. Century 进度。
2. 2-3 位小数秒显示。
3. 自定义周期进度条。
4. 背景透明度调节。

### 13.3 暂缓项
1. 多主题。
2. 菜单栏常驻图标。
3. 开机启动。
4. 多窗口。
5. 数据统计与历史回看。

## 14. 实现阶段建议

### Phase 1：最小闭环
1. 建立 macOS SwiftUI App。
2. 做出默认 1 位小数秒时间 + 日期 + 5 条内置进度。
3. 做出普通/悬浮切换。
4. 做出窗口尺寸与位置记忆。
5. 做出自定义固定区间进度条。

### Phase 2：可配置化
1. 设置界面。
2. 时间精度切换。
3. 条目显隐与排序。
4. 背景透明度。
5. 自定义进度条管理优化。

### Phase 3：进度系统升级
1. Century。
2. 自定义周期区间。
3. 自定义标签模板与更多周期规则。

## 15. 风险与关键决策

### 15.1 关键决策
1. 这是一个原生 macOS 小应用，不是 Widget。
2. 时间与进度条共享同一信息框架：当前时刻 + 当前周期消耗。
3. 进度计算必须基于真实日历区间。

### 15.2 潜在风险
1. 如果界面太“黑盒”，用户不知道每条进度表示什么，所以标签不能省掉。
2. 如果进度条太多，会从“极简”变成“拥挤”。因此默认只开 5 条最有体感的条目，并在小窗口下自动降级标签密度。
3. 默认开启 1 位小数秒会增强压迫感，这符合产品目标，但也必须允许用户一键降回秒级显示。

## 16. 首版结论

这个产品首版应该克制，不要做成“大而全”的时间仪表盘。最正确的方向是：

1. 用一个极简窗口持续显示“现在”。
2. 用 5 条最关键进度条显示“正在失去多少”，其中“本小时”负责短周期行动压力，“今日/本周/本月/今年”负责中长期尺度。
3. 用悬浮与缩放能力让它适配不同工作场景。

换句话说，它不是一个复杂效率系统，而是一个视觉化的时间压力装置。

## 17. 决策状态

### 17.1 已确认
1. 时间默认格式采用 `HH:mm:ss.S`，也就是默认显示 1 位小数秒。
2. Week 的一周起始日跟随系统地区与日历设置。
3. 第一版直接支持自定义进度条。
4. 第一版新增 `本小时` 作为第一条内置时间轴，并使用“`60` 分钟格子 + `15` 分钟主标签 + `5` 分钟次刻度”的设计。

### 17.2 仍待确认
1. Century 是否进入首版默认显示，还是先隐藏在设置中。
2. 窗口首次启动默认是普通模式，还是直接默认悬浮。

如果你暂时不特别指定，当前文档仍沿用这两个默认建议：
1. Century 先不默认展示。
2. 首次启动默认普通窗口，用户手动切换到悬浮。
